export const metadata = {
    title: "Project [SAF] - PayrollMgt",
    description: "Details about the Payroll Management project.",
  };

import React from "react";
import Link from 'next/link';

function PayrollMgt() {
    return (
        <div>
            
        </div>
    );
}

export default PayrollMgt;